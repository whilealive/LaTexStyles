% ==================================================================
% FILE     tikzStyle.sty
% MACHINE  all
% INFO     tikz macros etc.
%
% DATE     08.04.2017
% OWNER    Bischofberger
% ==================================================================

\ProvidesPackage{tikzStyle}

\RequirePackage{tikz}
\usetikzlibrary{calc,angles,intersections,arrows.meta}

% Shift previously defined coordinates. Usage:
% --------------------------------------------
% \begin{scope}[every coordinate/.style={shift={(x,y)}}]
%   \draw ([c]A) -- ([c]B);
% \end{scope}
% ------
\tikzset{c/.style={every coordinate/.try}}

% circle arc around point
\tikzset{partial ellipse/.style args={#1:#2:#3}{insert path={+ (#1:#3) arc (#1:#2:#3)}}}

% right angle, german style
% \RechterWinkel{A--B--C}[4mm][thick,red];
\NewDocumentCommand{\RechterWinkel}{ o m }
{
  \IfNoValueTF{#1}
  {
    \draw pic[pic text=., draw, angle eccentricity=.55] {angle=#2};
  }
  {
    \draw pic[pic text=., draw, angle radius=#1, angle eccentricity=.55] {angle=#2};
  }
}

% Point that won't get scaled
% args: #1 = coordinate, #2 = node text, #3 = node position
\newcommand*{\Point}[1]
{%
  \draw[fill] (#1) node[circle,fill,inner sep=1.3pt] {};
}

% nicer arrow tip default
\tikzset{>={Triangle[angle=45:4pt 5]}}

